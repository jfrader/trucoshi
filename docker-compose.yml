services:
  postgres:
    extends:
      file: docker-compose.base.yml
      service: postgres
  redis:
    extends:
      file: docker-compose.base.yml
      service: redis
  trucoshi:
    extends:
      file: docker-compose.base.yml
      service: trucoshi

volumes:
  node_modules:
